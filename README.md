# **🎯 CareerBot — Телеграмм-бот для выбора карьеры**

## 🛠 Инструменты для реализации:
> - Python 3.x  
> - [pyTelegramBotAPI](https://pypi.org/project/pyTelegramBotAPI/) для работы с Telegram  
> - SQLite или PostgreSQL для хранения данных пользователей и рекомендаций  
> - Markdown для форматирования сообщений бота  
> - JSON для структуры базы данных профессий и интересов

## 🧩 Структура проекта
```
M8L1/
├─ bot.py — основной файл для работы с Telegram
├─ config.py — хранение токена и настроек бота
└─ logic.py — логика рекомендаций и взаимодействие с базой
```

# **📋 План работы по проекту CareerBot**

## 1️⃣ Подготовка
- Определение требований и целевой аудитории (подростки, люди, которые меняют работу).  
- Сбор референсов (онлайн-тесты, AI-платформы, карьерные боты).  
- Определение уникальности проекта: Telegram-бот с персонализированными рекомендациями.

## 2️⃣ Настройка проекта и файлов
- Создать `config.py` — хранение токена и настроек.  
- Создать `career.db` — база данных SQLite для пользователей и профессий.  
- Создать `bot.py` — основной файл для Telegram-бота.  
- Создать `logic.py` — функции логики, генерации рекомендаций, меню.  

## 3️⃣ Проектирование базы данных (`career.db`)
- Таблица **Users**: `user_id`, `username`, `interests`.  
- Таблица **Recommendations**: `id`, `user_id`, `recommendation`.  
- Решить, какие данные можно добавлять позже (расширяемость базы).  

## 4️⃣ Реализация логики (`logic.py`)
- Написать функции:  
  - `generate_recommendation(interests)` — генерация списка профессий по интересам.  
  - `save_recommendation(user_id, recommendations)` — сохранение истории.  
  - `handle_user_interest(user_id, username, interest)` — обработка выбранного интереса.  
  - `build_main_menu()` и `build_interests_menu()` — построение интерактивного меню.  

## 5️⃣ Создание интерфейса бота (`bot.py`)
- Обработчики команд: `/start`.  
- Callback-обработчики для кнопок (главное меню, выбор интересов, рекомендации, назад).  
- Обработка текстовых сообщений для случаев, когда пользователь пишет вместо нажатия кнопки.  
- Отправка пользователю сообщений в удобочитаемом формате (список с дефисами и эмодзи).  

## 6️⃣ Тестирование демо-версии
- Проверка корректного отображения меню и кнопок.  
- Проверка генерации и сохранения рекомендаций.  
- Проверка работы с базой данных.  
- Тестирование разных сценариев взаимодействия с пользователем.  

## 7️⃣ Что можно использовать из старых проектов
- Структуру Telegram-бота на `pyTelegramBotAPI`.  
- Простейшие функции работы с SQLite (создание таблиц, сохранение данных).  

## 8️⃣ Что писать с нуля
- Логику персонализированных рекомендаций.  
- Меню и интерактивные кнопки под карьерного советчика.  
- Обработку интересов и сохранение истории пользователей.